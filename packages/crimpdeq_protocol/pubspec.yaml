name: crimpdeq_protocol
description: Tindeq Progressor BLE protocol layer — packet parsing, command encoding, event stream.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.10.3 <4.0.0"

dependencies:
  freezed_annotation: ^3.1.0

dev_dependencies:
  build_runner: ^2.4.13
  freezed: ^3.2.5
  test: ^1.25.0
